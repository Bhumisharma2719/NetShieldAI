# NetShield AI Frontend

React + Vite frontend with password login, optional Google login, and separate admin/analyst dashboard views.

## Setup

```bash
cd frontend
npm install
copy .env.example .env
npm run dev
```

Set `VITE_GOOGLE_CLIENT_ID` in `.env` to enable the Google button.
